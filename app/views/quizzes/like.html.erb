<div class="container" id="page-title">
  <h1 :style="styles">クイズに挑戦する</h1>
</div><br>
<div class="container">
  <h2 class="text-success">あなたが”いいね！”したクイズ一覧</h2>
</div><br>
<div class="container">
  <div class="card border-success">
    <div class="card-body">
      <% if @quizzes.present? %>
        <table class="table">
          <tbody>
            <% @quizzes.each do |quiz| %>
              <tr>
                <td>
                  <h3 class="h3"><%= quiz.title %><h3>
                  <h6>ジャンル：<%= quiz.genre %></h6>
                  <h6>作成ユーザー：<%= quiz.user.nickname %></h6>
                </td>
                <td>
                  <h6>いいね数 <%= quiz.likes.count %></h6>
                </td>
                <td>
                  <%= link_to "挑戦する", start_quiz_path(quiz), method: 'post', class: "btn btn-lg btn-info" %>
                </td>
              </tr>
            <% end %>
          </tbody>  
        </table>
      <% else %>
        <h2 class="card-text text-danger text-center">”いいね！”したクイズはありません。</h2>
      <% end %>
    </div>
  </div>
</div><br><br>